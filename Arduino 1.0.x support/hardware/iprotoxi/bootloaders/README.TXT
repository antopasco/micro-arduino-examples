The folder here should be "iprotoxi" and not "caterina", but because IDE does the needed reset only for boards
that have <board>.bootloader.path set to "caterina", let's use that name also here. (AvrdudeUploader.java is
the IDE source file that have this behavior hardcoded there.)
